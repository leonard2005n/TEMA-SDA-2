#pragma once

#include <stdio.h>

// Function that solves the zecond subtask
void subtask2(FILE *in, FILE *out);

// Function that solves the third subtask
void subtask3(FILE *in, FILE *out);

// Function that solves the forth subtask
void subtask4(FILE *in, FILE *out);

// Function that solves the fifth subtask
void subtask5(FILE *in, FILE *out);
